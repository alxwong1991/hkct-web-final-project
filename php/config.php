<?php

    $hostname = "localhost";
    $username = "root";
    $password = "";
    $database = "hkct_web_final_project";

    $conn = mysqli_connect($hostname, $username, $password, $database) or die("Database connection failed");
